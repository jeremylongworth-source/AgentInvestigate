interface:
  display_name: "Build Investigation Plan"
  short_description: "Draft bounded investigation plans"
  default_prompt: "Use $build-investigation-plan to draft a bounded investigation plan from this scoped matter."
policy:
  allow_implicit_invocation: true
