interface:
  display_name: "Build Allegations Matrix"
  short_description: "Build allegations matrices"
  default_prompt: "Use $build-allegations-matrix to build a workplace allegations matrix."
policy:
  allow_implicit_invocation: true
