interface:
  display_name: "Define Protected Assets"
  short_description: "Define protected assets"
  default_prompt: "Use $define-protected-assets to define protected assets for this physical security assessment."
policy:
  allow_implicit_invocation: true
