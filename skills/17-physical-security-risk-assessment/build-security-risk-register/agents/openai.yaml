interface:
  display_name: "Build Security Risk Register"
  short_description: "Build risk registers"
  default_prompt: "Use $build-security-risk-register to build a physical security risk register."
policy:
  allow_implicit_invocation: true
