interface:
  display_name: "Prepare Findings Matrix"
  short_description: "Prepare findings matrices"
  default_prompt: "Use $prepare-findings-matrix to prepare a findings matrix from these draft findings."
policy:
  allow_implicit_invocation: true
